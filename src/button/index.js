const mainButton = document.getElementById('main-button');

const handleButtonCLick = () => {
    // Do broker operations here
    console.log("clicked!");
}

mainButton.addEventListener('click', handleButtonCLick);
